import React from "react";



const Smoke= (props) =>(
// explicitly returning the div element
            <div>
            <p>No chance of a good smoke sesh today, rip your bong indoors today!</p> 
            </div>
        );
export default Smoke;